#include<stdio.h>
#include<string.h>

int lenth (char *p)
{
    int i;
for (int i = 0; *(p+i)='\0'; i++);
return(i);
}
char * reverse(char *p)
{
    int i,l;
    for (int i = 0; i != ; i++)
    {
        /* code */
    }
    
}
int main()
{
    
    return 0;
}
